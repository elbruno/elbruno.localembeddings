@inherits LayoutComponentBase

<div class="page">
    <main>
        <nav class="top-nav">
            <span class="nav-brand">🔍 LocalEmbeddings BlazorDemo</span>
            <EmbeddingHealthBadge IsReady="true" ModelName="all-MiniLM-L6-v2" />
        </nav>
        <article>
            @Body
        </article>
    </main>
</div>

<style>
    .page { display: flex; flex-direction: column; min-height: 100vh; }
    .top-nav { background: #0d6efd; color: #fff; padding: 10px 20px; display: flex; align-items: center; justify-content: space-between; }
    .nav-brand { font-size: 1.1rem; font-weight: 600; }
    article { padding: 24px; }
</style>
