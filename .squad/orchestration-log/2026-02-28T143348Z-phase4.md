# Orchestration Log: Phase 4 — 2026-02-28T14:33:48Z

**Phase:** 4  
**Agents:** Ash (Security), Parker (Performance), Lambert (QA)  
**Status:** COMPLETED ✅

## Tasks Dispatched

| Agent | Tasks | Description |
|-------|-------|-------------|
| Ash | SEC-002, SEC-007, SEC-008, SEC-009 | HttpClient lifecycle, sync-over-async docs, OnnxRuntime bump, ClipTokenizer file size guard |
| Parker | PERF-04, PERF-05 docs + residual allocation fix | Async factory documentation + GenerateAsync .ToList() fix |
| Lambert | 5 tests | SEC-002, SEC-009, SEC-007, PERF-04 coverage |

## Outcomes

**Ash — SEC-002 (HttpClient PooledConnectionLifetime):**
- Parameterless `ModelDownloader()` ctor uses `new SocketsHttpHandler { PooledConnectionLifetime = TimeSpan.FromMinutes(2) }`
- XML doc comment steers production users toward DI / `IHttpClientFactory`

**Ash — SEC-007 (Sync-over-async documentation):**
- `<remarks>` added to `LocalEmbeddingGenerator(LocalEmbeddingsOptions)` constructor warning about deadlock risk
- Inline `// Sync-over-async` comment at `.GetAwaiter().GetResult()` call site

**Ash — SEC-008 (OnnxRuntime 1.24.1 → 1.24.2):**
- Bumped in `ElBruno.LocalEmbeddings.csproj`, `ElBruno.LocalEmbeddings.ImageEmbeddings.csproj`, and `ElBruno.LocalEmbeddings.Tests.csproj`
- Test project pinned directly to prevent NU1605 downgrade warning-as-error

**Ash — SEC-009 (ClipTokenizer file size guard):**
- 50 MB guard added for both `vocabJsonPath` and `mergesTxtPath` before file reads
- Throws `InvalidOperationException` with file name and actual size in MB

**Parker — PERF-04/05 (Async factory docs):**
- `ServiceCollectionExtensions.AddLocalEmbeddings`: expanded `<remarks>` + two-part `<example>` block (DI usage + async pre-build pattern)
- `CreateAsync(LocalEmbeddingsOptions, CancellationToken)`: enriched `<remarks>` + example code block
- ImageEmbeddings `ServiceCollectionExtensions`: `<strong>Async-Safety Note</strong>` in remarks

**Parker — Residual allocation fix:**
- `GenerateAsync`: `var valuesList = values.ToList()` → `IList<string> valuesList = values as IList<string> ?? values.ToList()`

**Lambert — 5 tests:**
- `ModelDownloaderSecurityTests.cs`: 1 test appended (SEC-002 default ctor behavioral verification)
- `ClipTokenizerFileSizeTests.cs`: 2 new tests (SEC-009, one TDD-first, one happy path)
- `AsyncPatternTests.cs`: 2 new tests (SEC-007 CreateAsync existence, PERF-04 DI registration)
- SEC-009 TDD test passes after Ash's implementation; all other tests pass immediately

## Build Status

`dotnet build` — 0 errors, 0 warnings ✅

---

**Logged by:** Scribe  
**Timestamp:** 2026-02-28T14:33:48Z
