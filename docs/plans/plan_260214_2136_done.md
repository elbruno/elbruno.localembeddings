# Plan — Implement Roadmap Item #1 (Multi-TFM net8.0 + net10.0) ✅ DONE

Date: 2026-02-14 21:36  
**Status:** ✅ Completed (2026-02-14)

## Goal

Implement roadmap item [1. Multi-TFM Support (net8.0 + net10.0)](./roadmap_260213_0803.md#1-multi-tfm-support-net80--net100) by adding .NET 8 alongside .NET 10 for the main library and Kernel Memory companion package, including their test projects.

## Scope

- Update target frameworks in:
  - `src/ElBruno.LocalEmbeddings/ElBruno.LocalEmbeddings.csproj`
  - `src/ElBruno.LocalEmbeddings.KernelMemory/ElBruno.LocalEmbeddings.KernelMemory.csproj`
  - `tests/ElBruno.LocalEmbeddings.Tests/ElBruno.LocalEmbeddings.Tests.csproj`
  - `tests/ElBruno.LocalEmbeddings.KernelMemory.Tests/ElBruno.LocalEmbeddings.KernelMemory.Tests.csproj`
- Validate compatibility by running repository build and tests from root.
- Update roadmap status to mark this item as completed.

## Non-Goals

- Changing sample project target frameworks.
- Refactoring runtime behavior or adding new APIs.
- Adding conditional compilation unless required by compilation failures.

## Implementation Steps

1. Change each listed project from `<TargetFramework>net10.0</TargetFramework>` to `<TargetFrameworks>net8.0;net10.0</TargetFrameworks>`.
2. Run `dotnet build` and `dotnet test` from repository root to validate both TFMs compile and tests pass.
3. Update `docs/plans/roadmap_260213_0803.md` item #1 and related summary rows to show completion.

## Acceptance Criteria

- Both library projects multi-target `net8.0` and `net10.0`.
- Both test projects multi-target `net8.0` and `net10.0`.
- Root `dotnet build` and `dotnet test` succeed after the changes.
- Roadmap item #1 is marked done.

## Files Expected to Change

- `src/ElBruno.LocalEmbeddings/ElBruno.LocalEmbeddings.csproj`
- `src/ElBruno.LocalEmbeddings.KernelMemory/ElBruno.LocalEmbeddings.KernelMemory.csproj`
- `tests/ElBruno.LocalEmbeddings.Tests/ElBruno.LocalEmbeddings.Tests.csproj`
- `tests/ElBruno.LocalEmbeddings.KernelMemory.Tests/ElBruno.LocalEmbeddings.KernelMemory.Tests.csproj`
- `docs/plans/roadmap_260213_0803.md`
- `docs/plans/plan_260214_2136.md` (new)
