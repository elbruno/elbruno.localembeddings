# Plan — Microsoft.Extensions.VectorData Integration (#4)

**Created:** 2026-02-14 22:14  
**Scope:** Implement roadmap item #4 (`Microsoft.Extensions.VectorData Integration`) with minimal, production-ready changes.

## Goals

- Add a companion package `ElBruno.LocalEmbeddings.VectorData`.
- Provide DI extensions that register:
  - `IEmbeddingGenerator<string, Embedding<float>>` (existing core registration)
  - `VectorStore` and typed `VectorStoreCollection<TKey, TRecord>` integration points.
- Add focused tests for the new DI extensions.
- Update docs (`README`, API reference, DI guide, getting started) with usage guidance.
- Update roadmap status for completed item(s).

## Implementation Checklist

- [x] Scaffold new projects:
  - `src/ElBruno.LocalEmbeddings.VectorData`
  - `tests/ElBruno.LocalEmbeddings.VectorData.Tests`
- [x] Add package and project references for VectorData abstractions + core library.
- [x] Implement `AddLocalEmbeddingsWithVectorStore(...)` overloads in VectorData companion package.
- [x] Implement typed collection helper registration for `VectorStoreCollection<TKey, TRecord>`.
- [x] Add unit tests covering service registration and argument validation.
- [x] Add new projects to solution.
- [x] Update docs and roadmap to reflect implementation.
- [ ] Run full validation (`dotnet build`, `dotnet test`) and finalize.
